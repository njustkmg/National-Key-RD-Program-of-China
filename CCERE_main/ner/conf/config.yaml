# ??? is a mandatory value.
# you should be able to set it without open_dict
# but if you try to read it before it's set an error will get thrown.

# populated at runtime
cwd: 00
lan: "zh" # en

defaults:
    - predict
    - hydra/output: custom
    - hydra/model: bert   # [bert, lstmcrf]
